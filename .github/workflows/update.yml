name: Update Dojo

on: push

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: pwncollege/dojo-update@v1
        with:
          site: ${{ secrets.DOJO_BASE_URL }}
          dojo: shell~30f3c326
          update_code: ${{ secrets.UPDATE_CODE }}
